<?xml version="1.0" encoding="UTF-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- フォーカス時 -->
    <!--
    <item
    android:state_focused="true"
    android:state_enabled="true"
    android:state_pressed="false"
    android:drawable="@drawable/focused" />
    -->
    <!-- 無効状態 かつ フォーカス時 -->
    <!--
    <item
    android:state_focused="true"
    android:state_enabled="false"
    android:state_pressed="false"
    android:drawable="@drawable/disabled_and_focused" />
    -->
    <!-- 押下状態 -->
    <!-- <item
        android:background="#2A9C00"
        android:state_enabled="true"
        android:state_focused="true"
        android:state_pressed="true"/> -->
    <!-- 無効状態 -->
    <!--
    <item
    android:state_enabled="false"
    android:drawable="@drawable/disabled" />
    -->
    <!-- 通常状態 -->
    <!--
    <item
    android:background="#43AE1C" />
    -->
    <!--
    <item
    android:drawable="@drawable/normal" />
    -->

    <item
       android:state_pressed="true">
       	<color android:color="@color/btn_bg_press" />
    </item>
    <item
       android:state_pressed="false">
       <color android:color="@color/btn_bg_release" />
    </item>

</selector>